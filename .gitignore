# ============================================================
# CRITICAL: Private keys and secrets — NEVER commit these!
# ============================================================
# Solana private keypair files (generated on Day 0 by Member A)
*-wallet.json
*.DANGER_PRIVATE_DELETE_OR_MOVE

# Real environment variables (contains passwords and API keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ============================================================
# Dependencies and build outputs
# ============================================================
node_modules/
/.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/

# Rust / Anchor build artifacts (can be 1GB+, never commit)
target/
packages/contract/scope4/target/
*.so
*.d

# Always keep the safe template in git
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
test-ledger/

*.tsbuildinfo
